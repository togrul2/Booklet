apiVersion: v1
kind: Service
metadata:
  name: {{ include "booklet.fullname" . }}
  labels:
    {{- include "booklet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "booklet.selectorLabels" . | nindent 4 }}
