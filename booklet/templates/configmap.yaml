apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "booklet.fullname" . }}
  labels:
    {{- include "booklet.labels" . | nindent 4 }}
data:
  # kv format
  key: "value"
  $actual-config-file$: "name.properties"

  # file format
  name.properties: |
    field=value1,value2
