apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "booklet.fullname" . }}-test-connection"
  labels:
    {{- include "booklet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "booklet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
