spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/booklet-test
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}

  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: create-drop
